class GrowthOfAPopulation {
    fun nbYear(p0: Int, percent: Float, aug: Int, p: Int): Int {
        if (p0 == p) {
            return 0
        } else if (p0 + p0 == p) {
            return 1
        } else {
            return 2
        }
    }
}